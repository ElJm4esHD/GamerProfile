# Copiá este archivo como `backend/.env` y completá tu clave.
# El .env NUNCA va a git: está ignorado.
#
# Conseguí una clave gratis en https://aistudio.google.com/apikey

GEMINI_API_KEY=
# Opcional. Si Google deprecia el modelo, se cambia acá sin tocar código.
GEMINI_MODEL=gemini-2.0-flash
